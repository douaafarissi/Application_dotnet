@model IEnumerable<test.Models.Produit>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProduitID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProduitID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProduitID">Delete</a>
                   <a asp-action="ADD" asp-route-id="@item.ProduitID">AJOUTERITEM</a>
            </td>
        </tr>
}
    </tbody>
</table>
